#!/usr/bin/env python

def options(opt):
	return

def configure(conf):
	return

def build(bld):
	source = [
		'packedstore.cpp'
	]

	includes = [
		'../public',
		'../public/tier1'
	]

	libs = []

	bld.stlib(
		target = 'vpklib',
		features = 'c cxx',
		source = source,
		includes = includes,
		use = libs,
		subsystem = bld.env.MSVC_SUBSYSTEM
	)
